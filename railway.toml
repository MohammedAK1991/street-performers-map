[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @smp/shared-types build && pnpm --filter @smp/backend build && pnpm --filter @smp/frontend build && cp -r apps/frontend/dist apps/backend/dist/"
watchPatterns = ["apps/backend/**", "apps/frontend/**", "packages/**"]

[deploy]
startCommand = "cd apps/backend && pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
PORT = { default = "3001" }